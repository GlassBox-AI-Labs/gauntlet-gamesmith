interface:
  display_name: "Ship"
  short_description: "Test, sync, and ship changes as a PR into main"
  default_prompt: "Use $ship to test, sync with the latest main, resolve conflicts, and push this workspace as a feature branch with a pull request into main."
